# 复制为 mcp.env，填入真实值（mcp.env 已在 .gitignore 中，不会提交）
# 然后在 mcp.json 的 mysql / github 段替换对应占位符

MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=admin
MYSQL_PASSWORD=你的数据库密码
MYSQL_DATABASE=u857194726_c168site

# GitHub → Settings → Developer settings → Personal access tokens
# 权限建议：repo, read:org, workflow（按需）
GITHUB_PAT=ghp_xxxxxxxxxxxxxxxxxxxx
