---
description: 双语（i18next）与响应式排版（clamp + 62.5% rem 基准），前端管 UI、后端与库表管数据
alwaysApply: true
---

# 双语网页（最易维护）

## 核心原则

- **前端**：只负责 **UI 文案** 的翻译与切换（**i18next** + **react-i18next**）。
- **后端（PHP API）**：只负责 **业务数据**（JSON），不在接口里承担页面级 UI 文案体系。
- **数据库**：需要对用户展示且随语言变化的内容，用 **字段分身**（例如 `title_zh` / `title_en`），由接口原样返回；**展示用哪一种语言由前端**根据当前语言选字段或组装视图。

## UI 翻译（i18next）

- 使用 **i18next**，文案集中在资源文件，例如：
  - `frontend/src/locales/zh.json`
  - `frontend/src/locales/en.json`
- 键名用 **稳定英文路径**（如 `auth.loginTitle`、`common.submit`），**禁止**在 React 组件里散落硬编码的中英文字符串作为长期方案。
- 按钮、标题、校验提示、空状态等 **一律走 `t('…')`**。

## 数据内容（数据库）

- 需要双语的 **业务内容字段**：采用 `*_zh` / `*_en`（或项目统一前缀）成对出现，由迁移/SQL 与 API 读写保持一致。
- API 的 `data` 中可同时返回 `title_zh` 与 `title_en`（或列表项内嵌对象），**不在 PHP 里拼接最终展示句**；若将来需要服务端按语言裁剪，再单独约定 `Accept-Language` 或 `lang` 查询参数，仍保持「数据字段清晰、无 HTML」。

---

# 响应式排版（与 rem 基准）

## HTML 根字号

- 在全局样式中设定：**`html { font-size: 62.5%; }`**（在默认 16px 下约为 **10px/rem 换算基准**，便于与下表 px 对照：`1rem ≈ 10px` 量级，具体以浏览器计算为准）。

## 字号层级（使用 `clamp()`）

以下 **rem** 与 **clamp** 与规格表一致；用途列便于实现时选用对应语义 class。

| 层级 | 级别 | px 范围 | rem 范围 | clamp | 建议用途 |
|------|------|---------|----------|--------|----------|
| 展示层 | 超大展示 | 48–64 | 4.8–6.4 | `clamp(4.8rem, 2.5vw + 1.6rem, 6.4rem)` | 总资产、极大金额等 |
| 标题层 | H1 | 24–32 | 2.4–3.2 | `clamp(2.4rem, 1.25vw + 0.8rem, 3.2rem)` | 页面主标题（如交易仪表盘） |
| 标题层 | H2 | 20–24 | 2.0–2.4 | `clamp(2.0rem, 0.625vw + 1.2rem, 2.4rem)` | 模块/卡片主标题（如趋势图） |
| 标题层 | H3 | 16–18 | 1.6–1.8 | `clamp(1.6rem, 0.3125vw + 1.2rem, 1.8rem)` | 卡片内次级标题 |
| 正文层 | Base | 14–16 | 1.4–1.6 | `clamp(1.4rem, 0.3125vw + 1.0rem, 1.6rem)` | 正文、表格、表单输入 |
| 正文层 | Small | 12–13 | 1.2–1.3 | `clamp(1.2rem, 0.15vw + 1.0rem, 1.3rem)` | 侧栏、表单 label、说明文案 |
| 辅助层 | Tiny | 10–11 | 1.0–1.1 | `clamp(1.0rem, 0.15vw + 0.8rem, 1.1rem)` | 图表轴、时间戳、页脚版权等 |
| 辅助层 | Label | 11 固定 | 1.1rem | **`1.1rem`（固定）** | 状态标签（如「已付」「待处理」）配合 **字重加粗** |

## 实现提示

- 在 Tailwind 中可用 **`text-[clamp(...)]`** 或 `@layer utilities` / `@layer components` 封装 `.text-display`、`.text-h1` 等，避免在 JSX 里使用 **inline style**。
- 保持 **13 寸无横向滚动**：大标题与 `clamp` 搭配 `max-w-*`、`overflow-x-hidden` 等布局约束，与现有全局规则一致。
