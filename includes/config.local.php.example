<?php
/**
 * EC2 / 本地覆盖数据库（复制为 config.local.php，已在 .gitignore）
 *
 *   cp includes/config.local.php.example includes/config.local.php
 */
$host = '127.0.0.1';
$dbname = 'u857194726_c168site';
$dbuser = 'admin';
$dbpass = 'C168_site';

// Transaction Payment realtime (SSE hub). Empty secret = disabled.
// Must match services/tx-realtime/.env TX_REALTIME_SECRET
$tx_realtime_secret = '';
$tx_realtime_publish_url = 'http://127.0.0.1:3911/publish';
// Optional explicit toggle; omit or null = enabled when secret is non-empty
// $tx_realtime_enabled = true;
