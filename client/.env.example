# 与 PHP 同域部署时留空，接口使用相对路径 /api/...
VITE_API_BASE=

# 仅本地 npm run dev：登录成功跳转到带 PHP 的站点根，否则 session 在另一端口无效
# VITE_DEV_POST_LOGIN_BASE=http://127.0.0.1

# 仅 Vite 开发服代理用（不暴露到浏览器，写在 client/.env.development）：
# DEV_PHP_ORIGIN=http://127.0.0.1
