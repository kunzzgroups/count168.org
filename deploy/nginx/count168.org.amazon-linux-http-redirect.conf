# count168.org — HTTP -> HTTPS redirect (used when certbot SSL is active)
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name count168.org www.count168.org;
    return 301 https://$host$request_uri;
}
